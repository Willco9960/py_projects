Balance = float(input("Amount owed: $"))
APR = float(input("APR: "))

Monthly = APR / 12
MinimumV = Monthly / 100
rounded = round(Monthly, 3)
print(f"Monthly percentage rate:",rounded)

Minimum = MinimumV * Balance
print(f"Minimum payment: ${round(Minimum, 2)}")
